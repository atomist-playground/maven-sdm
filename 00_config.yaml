configuration:
  logging:
    level: info
  sdm:
    cache:
      enabled: true
      path: /cache
  env:
    version_goal_image: 'atomist/version-goal:0.1.0-master.20191108121808'

---

immaterial:

  test:
  - not:
      is_material_change:
        files:
        - pom.xml
        - package.json
        extensions:
        - ts
        - js
        - java
        - html
        patterns:
        - '**/Dockerfile'

  goals: lock
